"use client";
import { FormEvent, useState } from "react";
import styles from "./page.module.css";
export function ChargeManager({ id, amount, dueDate }: { id: string; amount: number; dueDate: string }) { const [open, setOpen] = useState(false); async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); await fetch(`/api/charges/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(new FormData(event.currentTarget))) }); window.location.reload(); } async function voidCharge() { const reason = window.prompt("Why should this monthly charge be voided?"); if (!reason || !window.confirm("Void this charge?")) return; await fetch(`/api/charges/${id}`, { method: "DELETE", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ reason }) }); window.location.reload(); } return <span className={styles.adminRowActions}><button className={styles.editButton} onClick={() => setOpen(true)}>Edit</button><button className={styles.voidButton} onClick={voidCharge}>Void</button>{open && <div className={styles.modalBackdrop}><form className={styles.modal} onSubmit={save}><div className={styles.modalHeading}><h3>Edit monthly charge</h3><button type="button" className={styles.closeButton} onClick={() => setOpen(false)}>×</button></div><label>Amount<input name="amount" type="number" min="1" defaultValue={amount} /></label><label>Due date<input name="dueDate" type="date" defaultValue={dueDate} /></label><button className={styles.primaryButton}>Save charge</button></form></div>}</span>; }